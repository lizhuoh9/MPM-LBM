# Step 60 Controlled Canonical Moving-Boundary Duration Ramp

Step 60 extends the Step 59 canonical `FSIDriver3D` real smoke result into a short controlled duration ramp.

## Scope

The exercised implementation owner remains:

- `src/mpm_lbm/sim/drivers/fsi_driver.py`

The matrix runner imports the canonical driver and calls:

```python
driver.run()
```

Required rows:

- `canonical_driver_moving_boundary_engineering_16_3step`
- `canonical_driver_moving_boundary_engineering_16_5step`
- `canonical_driver_penalty_16_5step`

All required rows use `n_grid = 16`, `n_particles = 512`, `mpm_substeps_per_lbm_step = 1`, `geometry_type = box`, disabled VTK output, disabled particle output, disabled quality checks, static boundary motion, disabled wall-velocity application, static geometry motion, and disabled geometry-motion application.

The optional `canonical_driver_moving_boundary_engineering_32_1step_optional` config is checked in but disabled by default.

## Evidence

The Step 60 evidence is generated by:

- `baseline_tests/run_step60_duration_ramp_matrix.py`
- `baseline_tests/run_step60_duration_ramp_quality.py`
- `baseline_tests/run_step60_output_guard.py`
- `baseline_tests/run_step60_step59_regression_guard.py`
- `baseline_tests/run_step60_artifact_manifest.py`

The committed artifacts live under:

- `outputs/step60_driver_runs/`
- `outputs/step60_duration_ramp_matrix/`
- `outputs/step60_duration_ramp_quality/`
- `outputs/step60_output_guard/`
- `outputs/step60_step59_regression_guard/`
- `outputs/step60_artifact_manifest/`

## Result

All three required rows passed:

| Row | LBM steps | Stable | Elapsed seconds |
| --- | --- | --- | --- |
| `canonical_driver_moving_boundary_engineering_16_3step` | 3 | true | 115.28821250000328 |
| `canonical_driver_moving_boundary_engineering_16_5step` | 5 | true | 299.3498921999999 |
| `canonical_driver_penalty_16_5step` | 5 | true | 215.405316999997 |

The matrix summary reports `duration_ramp_matrix_pass = true`, no NaN/Inf rows, and `legacy_driver_module_used_count = 0`.

## Output Boundary

Each Step 60 driver run directory may contain only:

- `driver_config.json`
- `geo_all_fluid_16.dat`
- `diagnostics_timeseries.csv`
- `diagnostics_timeseries.npz`

The output guard rejects VTR output, particle NPY output, large files, private absolute paths in Step 60 outputs/logs, edits under `external/taichi_LBM3D`, and edits under `data/real_geometry_candidates`.

## Non-Goals

Step 60 does not activate runtime geometry, wall-velocity application, geometry motion, prescribed boundary motion, 48^3 or 64^3 rows, long-duration rows, required link-area rows, real geometry, propulsion validation, real squid behavior validation, mesh-convergence proof, viscosity/tau migration, heavy visualization output, particle output, or deployment-readiness claims.

## Interpretation

Passing Step 60 means the existing canonical driver can run a short finite/bounded 16^3 duration ramp for moving-boundary engineering and penalty modes while preserving Step 59 and output-guard boundaries.

Passing Step 60 does not establish larger-grid behavior, runtime-geometry behavior, wall-velocity behavior, or final physical correctness.
